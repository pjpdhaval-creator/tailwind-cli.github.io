method - 1
git  -repo create
enter 
git clone <link>
enter
git add .   / git  add index.html  (enter)
git commit -m "msg" + (enter)
git push origin main + (enter)

#tailwind css ClI
<h1>Website</h1>
1. copy -npm install css @import "tailwindcss";
2. link css in index.html and src="./output.css"
3. copy @import "tailwindcss";

 



<h1>VS code</h1>
1. open  Terminal
2. paste cmd
3. Enter
4. create index.html and input.css
5. link css in index.html and src="./output.css"
6. open package.json file and type 
<code>
"scripts": {"start": "npx @tailwindcss/cli -i ./input.css -o ./output.css --watch"}
<code>
7. input.css file paste  @import "tailwindcss";